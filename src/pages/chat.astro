---
// /chat — AI launcher. UI reproduced EXACTLY from the supplied reference
// bundle ("Comanager Landing (standalone).html"): rendered markup + tokens
// are used verbatim (src/chatref/*), deliberately NOT the shared Campaign
// layout so no site CSS interferes. Fonts are the same families/weights as
// the bundle's, served from our self-hosted WOFF2.
// Kept (non-UI): canonical, PostHog + Clarity + ab-lp stamping, signup_initiate.
export const prerender = false;

import tokensCss from '../chatref/tokens.css?raw';
import baseCss from '../chatref/base.css?raw';
import markup from '../chatref/markup.html?raw';
import SiteNav from '../components/SiteNav.astro';
import SiteFooter from '../components/SiteFooter.astro';

const SIGNIN = 'https://app.comanager.com/sign-in';
const SIGNUP = 'https://app.comanager.com/sign-up';
---
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>comanager — What's next for your music?</title>
<meta name="description" content="Your AI music manager. Ask what's next for your music — pitching, production, content, booking, release plans — and a full team of managers gets it done." />
<meta name="theme-color" content="#060605" />
<link rel="icon" type="image/png" href="/favicon.png" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
<link rel="canonical" href="https://comanager.com/" />
<style is:inline set:html={tokensCss}></style>
<style is:inline set:html={baseCss}></style>
</head>
<body>
<SiteNav
  links={[
    { label: 'How it works', href: '/#how' },
    { label: 'Pricing', href: '/#pricing' },
  ]}
  actions={[
    { label: 'Sign in', href: SIGNIN, kind: 'link', blank: true },
    { label: 'Sign up', href: SIGNUP, kind: 'pill', blank: true },
  ]}
/>
<Fragment set:html={markup} />
<SiteFooter
  links={[
    { label: 'Privacy', href: SIGNIN, blank: true },
    { label: 'Terms', href: SIGNIN, blank: true },
    { label: 'Contact', href: SIGNIN, blank: true },
  ]}
/>

<!-- Typewriter (the bundle animated this at runtime; frozen frame reset) -->
<script is:inline>
  (function () {
    var tw = document.querySelector('[data-tw]');
    var ta = document.querySelector('textarea.cm-input');
    if (!tw || !ta) return;
    var overlay = tw.parentElement;
    var phrases = [
      'Who should produce my next single?',
      'Write a pitch for my new single',
      'Plan a week of content in minutes',
      'Which playlists fit my sound?',
      'Map out my release timeline',
      'Book my first hometown show',
    ];
    var pi = 0, ci = 0, deleting = false;
    function tick() {
      if (ta.value || document.activeElement === ta) {
        overlay.style.opacity = '0';
        setTimeout(tick, 500);
        return;
      }
      overlay.style.opacity = '1';
      var full = phrases[pi];
      ci += deleting ? -1 : 1;
      tw.textContent = full.slice(0, ci);
      var delay = deleting ? 30 : 55;
      if (!deleting && ci === full.length) { deleting = true; delay = 1700; }
      else if (deleting && ci === 0) { deleting = false; pi = (pi + 1) % phrases.length; delay = 350; }
      setTimeout(tick, delay);
    }
    setTimeout(tick, 600);
    // Send button: off-white (primary) only while the user has typed something.
    var sendBtn = document.querySelector('button[aria-label="Send"]');
    function updateSend() {
      if (!sendBtn) return;
      if (ta.value.trim()) { sendBtn.style.background = 'rgb(224, 219, 191)'; sendBtn.style.color = 'rgb(6, 6, 5)'; }
      else { sendBtn.style.background = 'rgb(47, 46, 41)'; sendBtn.style.color = 'rgb(143, 139, 122)'; }
    }
    ta.addEventListener('input', updateSend);
    updateSend();

    // Every interaction hands off to the app so the platform can continue it.
    var APP = 'https://app.comanager.com/sign-in';
    function go(promptText, source) {
      var u = new URL(APP);
      if (promptText) u.searchParams.set('prompt', promptText);
      u.searchParams.set('utm_source', 'comanager.com');
      u.searchParams.set('utm_medium', 'website');
      u.searchParams.set('utm_campaign', location.pathname === '/' ? 'home' : 'chat');
      u.searchParams.set('utm_content', source);
      var abv = (document.cookie.match(/ab-lp=([^;]+)/) || [])[1];
      if (abv) u.searchParams.set('utm_term', 'ab-' + abv);
      if (window.posthog) posthog.capture('signup_initiate', { ab_lp: abv || 'none', cta_href: source, cta_text: (promptText || '').slice(0, 120) });
      window.location.href = u.toString();
    }
    // Send (submit) → carries the typed prompt
    var form = ta.closest('form');
    if (form) form.addEventListener('submit', function (e) {
      e.preventDefault();
      go(ta.value.trim(), 'chat-input');
    });
    // Attach (+) button → straight to sign-in (keeps any typed prompt along)
    var attach = document.querySelector('button[aria-label="Add attachment"]');
    if (attach) attach.addEventListener('click', function () {
      go(ta.value.trim(), 'chat-attach');
    });
    // Enter (without Shift) in the textarea also sends
    ta.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); go(ta.value.trim(), 'chat-input'); }
    });
  })();
</script>

<!-- Analytics: PostHog + Microsoft Clarity (same wiring as the rest of the site) -->
<script is:inline>
    !function(t,e){var o,n,p,r;e.__SV||(window.posthog && window.posthog.__loaded)||(window.posthog=e,e._i=[],e.init=function(i,s,a){function g(t,e){var o=e.split(".");2==o.length&&(t=t[o[0]],e=o[1]),t[e]=function(){t.push([e].concat(Array.prototype.slice.call(arguments,0)))}}p||((p=t.createElement("script")).type="text/javascript",p.crossOrigin="anonymous",p.async=!0,p.src=s.api_host.replace(".i.posthog.com","-assets.i.posthog.com")+"/static/array.js",p.onerror=function(){p=null},(r=t.getElementsByTagName("script")[0]).parentNode.insertBefore(p,r));var u=e;for(void 0!==a?u=e[a]=[]:a="posthog",u.people=u.people||[],u.toString=function(t){var e="posthog";return"posthog"!==a&&(e+="."+a),t||(e+=" (stub)"),e},u.people.toString=function(){return u.toString(1)+".people (stub)"},o="fo po init Fo Oo qo Zs Lo Bo Ro capture Do vo Go calculateEventProperties Vo register register_once register_for_session unregister unregister_for_session Ko Ao Zo getFeatureFlag getFeatureFlagPayload getFeatureFlagResult getAllFeatureFlags isFeatureEnabled reloadFeatureFlags updateFlags updateEarlyAccessFeatureEnrollment getEarlyAccessFeatures on onFeatureFlags onSurveysLoaded onSessionId getSurveys getActiveMatchingSurveys renderSurvey displaySurvey cancelPendingSurvey canRenderSurvey canRenderSurveyAsync Yo identify setPersonProperties unsetPersonProperties group resetGroups setPersonPropertiesForFlags resetPersonPropertiesForFlags setGroupPropertiesForFlags resetGroupPropertiesForFlags reset Xo shutdown setIdentity clearIdentity get_distinct_id getGroups get_session_id get_session_replay_url alias set_config startSessionRecording stopSessionRecording sessionRecordingStarted captureException addExceptionStep captureLog startExceptionAutocapture stopExceptionAutocapture loadToolbar get_property getSessionProperty Qo Uo createPersonProfile setInternalOrTestUser Jo Eo il opt_in_capturing opt_out_capturing has_opted_in_capturing has_opted_out_capturing get_explicit_consent_status is_capturing clear_opt_in_out_capturing Ho debug Js mn getPageViewId captureTraceFeedback captureTraceMetric Co".split(" "),n=0;n<o.length;n++)g(u,o[n]);e._i.push([i,s,a])},e.__SV=1)}(document,window.posthog||[]);
    posthog.init('phc_zuvvf2s5CkPF8bdxfPndR4e9WkZjJbWKwxDdATJE96VZ', {
        api_host: 'https://us.i.posthog.com',
        defaults: '2026-05-30',
        person_profiles: 'identified_only',
    })
</script>
<script is:inline>
    (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
    })(window, document, "clarity", "script", "ub9vo9s8gv");
</script>
<script is:inline>
  (function () {
    var m = document.cookie.match(/(?:^|;\s*)ab-lp=([^;]+)/);
    var variant = m ? decodeURIComponent(m[1]) : null;
    if (variant) {
      if (window.posthog) posthog.register({ ab_lp: variant });
      if (window.clarity) window.clarity('set', 'ab_lp', variant);
    }
    document.addEventListener('click', function (e) {
      var a = e.target && e.target.closest && e.target.closest('a[href*="app.comanager.com"]');
      if (a && window.posthog) {
        posthog.capture('signup_initiate', {
          ab_lp: variant || 'none',
          cta_href: a.getAttribute('href'),
          cta_text: (a.textContent || '').trim().slice(0, 60),
        });
      }
    }, true);

    // UTM decorator (same schema as the rest of the site)
    var page = location.pathname === '/' ? 'home'
      : location.pathname.replace(/^\/+|\/+$/g, '').replace(/\//g, '-');
    function slug(s) {
      return (s || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '').slice(0, 48) || 'link';
    }
    document.querySelectorAll('a[href*="app.comanager.com"], a[href*="venicemusic.co"]').forEach(function (a) {
      try {
        var u = new URL(a.getAttribute('href'), location.origin);
        if (u.searchParams.get('utm_source')) return;
        u.searchParams.set('utm_source', 'comanager.com');
        u.searchParams.set('utm_medium', 'website');
        u.searchParams.set('utm_campaign', page);
        u.searchParams.set('utm_content', a.dataset.utm
          || (u.searchParams.get('prompt') ? 'card-' + slug(u.searchParams.get('prompt')) : slug(a.textContent)));
        if (variant) u.searchParams.set('utm_term', 'ab-' + variant);
        a.setAttribute('href', u.toString());
      } catch (err) {}
    });
  })();
</script>
</body>
</html>
